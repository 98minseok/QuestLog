# Keep the existing unauthenticated local workflow.
VITE_AUTH_MODE=dev

# Use these values instead to authenticate through the imported local Keycloak realm.
# VITE_AUTH_MODE=keycloak
# VITE_KEYCLOAK_URL=http://localhost:18080
# VITE_KEYCLOAK_REALM=questlog
# VITE_KEYCLOAK_CLIENT_ID=questlog-frontend
